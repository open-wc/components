# Publish as `@open-wc/components`

The open source release will publish the package as `@open-wc/components` rather than the current `@finum/data-table` identity. This positions the library as a general Open Web Components package and accepts the required documentation and metadata churn before publication, instead of preserving the existing Finum-specific package name.
