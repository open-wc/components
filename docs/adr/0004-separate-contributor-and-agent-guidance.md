# Separate contributor and agent guidance

The open source release keeps public contributor rules in `CONTRIBUTING.md` and agent/operator workflow rules in `AGENTS.md` plus `docs/agents/`. This avoids mixing internal automation guidance into public contribution docs while still giving agents a stable place for repository-specific workflow constraints.
